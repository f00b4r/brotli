{
  "builders": [
    {
      "type": "docker",
      "image": "debian:buster-slim",
      "commit": true
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "apt-get update",
        "apt-get dist-upgrade -y",
        "apt-get install --no-install-recommends --no-install-suggests -y gnupg1 apt-transport-https ca-certificates git gcc wget make sudo libpcre3 libpcre3-dev zlib1g zlib1g-dev openssl libssl-dev"
      ]
    }
  ],
  "post-processors": [
    {
      "type": "docker-tag",
      "repository": "pwnlabs/brotli",
      "tag": "buster"
    }
  ]
}
